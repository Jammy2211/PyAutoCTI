# .readthedocs.yml

version: 2

build:
   apt_packages:
      - libgsl-dev

sphinx:
  configuration: docs/conf.py

python:
  version: 3.8
  install:
  - requirements: docs/requirements.txt

formats:
  []